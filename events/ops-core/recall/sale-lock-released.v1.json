{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "events/ops-core/recall/sale-lock-released.v1.json",
  "title": "Sale Lock Released Event v1",
  "description": "Published when sale lock is released after owner-approved recovery. Consumers must re-check sellable status before reopening commerce flows.",
  "$comment": "Sources: PACK-01 Sale Lock release boundary. TODO: final sale-lock release transition model is not specified; event only signals the release record.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "eventId": {
      "type": "string",
      "minLength": 1,
      "description": "Unique event identifier."
    },
    "eventType": {
      "type": "string",
      "const": "ops-core.recall.sale-lock-released.v1",
      "description": "Stable event type including source, domain, name, and v1 suffix."
    },
    "eventVersion": {
      "type": "string",
      "const": "1.0",
      "description": "Event contract version."
    },
    "occurredAt": {
      "type": "string",
      "format": "date-time",
      "description": "Time the domain event occurred."
    },
    "source": {
      "type": "string",
      "const": "ops-core",
      "description": "System that owns and emits this event."
    },
    "correlationId": {
      "type": "string",
      "minLength": 1,
      "description": "Cross-system correlation identifier."
    },
    "causationId": {
      "type": "string",
      "minLength": 1,
      "description": "Optional parent command, event, or workflow identifier."
    },
    "idempotencyKey": {
      "type": "string",
      "minLength": 1,
      "description": "Required when emitted from retryable or high-risk command; optional for derived read-model notifications."
    },
    "data": {
      "allOf": [
        {
          "$ref": "../../../schemas/ops/sale-lock.schema.json"
        },
        {
          "type": "object",
          "required": [
            "sale_lock_id",
            "scope_type",
            "scope_id",
            "active",
            "effective_to"
          ],
          "properties": {
            "active": {
              "const": false
            }
          }
        }
      ]
    }
  },
  "required": [
    "eventId",
    "eventType",
    "eventVersion",
    "occurredAt",
    "source",
    "correlationId",
    "data"
  ]
}
