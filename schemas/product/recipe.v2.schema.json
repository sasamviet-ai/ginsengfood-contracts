{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schemas/product/v2/recipe.schema.json",
  "title": "Recipe v2",
  "description": "Recipe v2 contract using the canonical ops-core lifecycle; readiness, usage lock, supersession, and guard outcomes are separate axes and are not inferred from lifecycle_status.",
  "$comment": "Source: owner-approved OD-B9-FORMULA-STATUS-001 option A. Runtime remains blocked by X04 auth/audience review.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "recipe_id": { "type": "string", "minLength": 1, "description": "Recipe identifier." },
    "sku_id": { "type": "string", "minLength": 1, "description": "SKU governed by this recipe." },
    "recipe_version": { "type": "string", "minLength": 1, "description": "Recipe version label." },
    "formula_version_id": { "type": "string", "minLength": 1, "description": "Formula Version used by the recipe." },
    "bom_id": { "type": "string", "minLength": 1, "description": "BOM governed by the recipe." },
    "lifecycle_status": {
      "type": "string",
      "enum": ["DRAFT", "PENDING_APPROVAL", "APPROVED", "APPROVED_SEED_BASELINE", "ACTIVE_OPERATIONAL", "RETIRED", "REJECTED"],
      "description": "Canonical Recipe/Formula lifecycle status. Only ACTIVE_OPERATIONAL is eligible for production-order recipe resolution."
    },
    "formula_kind": {
      "type": "string",
      "enum": ["PILOT_PERCENT_BASED", "FIXED_QUANTITY_BATCH"],
      "description": "Formula calculation kind; this is not a lifecycle status."
    },
    "owner_approval_ref": { "$ref": "../common/owner-decision-ref.schema.json" },
    "effective_date": { "type": "string", "format": "date", "description": "Date when the recipe becomes effective." },
    "evidence_refs": { "type": "array", "items": { "$ref": "../common/evidence-ref.schema.json" } },
    "audit_refs": { "type": "array", "items": { "$ref": "../common/audit.schema.json" } }
  },
  "required": ["recipe_id", "sku_id", "recipe_version", "formula_version_id", "lifecycle_status", "formula_kind"]
}
